<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.thinkjoy.saas.dao.IJwCourseBaseInfoDAO">
<!--
insert
update
updateNull
deleteById
deleteByCondition
deleteByProperty
fetch
findOne
findList
findAll
queryPage
like
queryList
queryOne
count
selectMaxId
updateOrSave
selectOne
selectList
-->
    <sql id="Base_Column_List">
        id as id,
        tn_id as tnId,
        course_name as courseName,
        course_type as courseType,
        grade as grade
    </sql>
    <insert id="insert" parameterType="JwCourseBaseInfo">
        INSERT INTO saas_jw_course_base_info (
        tn_id,
        course_name,
        course_type,
        grade
        ) VALUES (
        #{tnId},
        #{courseName},
        #{courseType},
        #{grade}
        )
        <selectKey resultType="java.lang.Long" keyProperty="id">
        SELECT LAST_INSERT_ID() AS ID
        </selectKey>
    </insert>

    <!-- 更新 -->
    <update id="update" parameterType="JwCourseBaseInfo">
        UPDATE saas_jw_course_base_info
        <trim prefix="SET" suffixOverrides=",">
            <if test="tnId!=null">
                tn_id = #{tnId},
            </if>
            <if test="courseName!=null">
                course_name = #{courseName},
            </if>
            <if test="courseType!=null">
                course_type = #{courseType},
            </if>
            <if test="grade!=null">
                grade = #{grade},
            </if>
        </trim>
        WHERE
        id = #{id}
    </update>

    <update id="updateMap">
        UPDATE saas_jw_course_base_info
        <trim prefix="SET" suffixOverrides=",">
            <if test="map.tnId!=null">
                tn_id = #{map.tnId},
            </if>
            <if test="map.courseName!=null">
                course_name = #{map.courseName},
            </if>
            <if test="map.courseType!=null">
                course_type = #{map.courseType},
            </if>
            <if test="map.grade!=null">
                grade = #{map.grade},
            </if>
        </trim>
        WHERE
        id = #{map.id}
    </update>

    <update id="updateByCondition">
        UPDATE saas_jw_course_base_info
        <trim prefix="SET" suffixOverrides=",">
            <if test="update.tnId!=null">
                tn_id = #{update.tnId},
            </if>
            <if test="update.courseName!=null">
                course_name = #{update.courseName},
            </if>
            <if test="update.courseType!=null">
                course_type = #{update.courseType},
            </if>
            <if test="update.grade!=null">
                grade = #{update.grade},
            </if>
        </trim>

        <trim prefix="WHERE" prefixOverrides="AND | OR">
            <if test="condition.id!=null">
                AND id = #{condition.id}
            </if>
            <if test="condition.tnId!=null">
                AND tn_id = #{condition.tnId}
            </if>
            <if test="condition.courseName!=null">
                AND course_name = #{condition.courseName}
            </if>
            <if test="condition.courseType!=null">
                AND course_type = #{condition.courseType}
            </if>
            <if test="condition.grade!=null">
                AND grade = #{condition.grade}
            </if>
        </trim>
    </update>

    <insert id="insertMap" >
        INSERT INTO saas_jw_course_base_info (
                tn_id,
                course_name,
                course_type,
                grade
        ) VALUES (
                    #{map.tnId},
                    #{map.courseName},
                    #{map.courseType},
                    #{map.grade}
        )
    </insert>


    <update id="updateNull" parameterType="JwCourseBaseInfo">
        UPDATE saas_jw_course_base_info
        <trim prefix="SET" suffixOverrides=",">
            tn_id = #{tnId},
            course_name = #{courseName},
            course_type = #{courseType},
            grade = #{grade},
        </trim>
        WHERE
        id = #{id} 
    </update>

    <!-- 按Id删除 -->
    <delete id="deleteById" parameterType="java.lang.Integer">
        DELETE FROM saas_jw_course_base_info
        <trim prefix="WHERE" prefixOverrides="AND | OR">
            AND id = #{id}
        </trim>
    </delete>


    <delete id="deleteByCondition" parameterType="java.util.Map">
        DELETE FROM saas_jw_course_base_info
        <trim prefix="WHERE" prefixOverrides="AND | OR">
            <if test="id!=null">
                AND id = #{id}
            </if>
            <if test="tnId!=null">
                AND tn_id = #{tnId}
            </if>
            <if test="courseName!=null">
                AND course_name = #{courseName}
            </if>
            <if test="courseType!=null">
                AND course_type = #{courseType}
            </if>
            <if test="grade!=null">
                AND grade = #{grade}
            </if>
        </trim>
    </delete>

    <delete id="deleteByProperty" parameterType="java.util.Map">
        DELETE FROM saas_jw_course_base_info WHERE
        ${property} = #{value}
    </delete>

    <select id="fetch" parameterType="java.lang.Integer" resultType="JwCourseBaseInfo">
        SELECT
        <include refid="Base_Column_List" />
        FROM saas_jw_course_base_info
        <trim prefix="WHERE" prefixOverrides="AND | OR">
            AND id = #{id}
        </trim>
    </select>

    <select id="findOne" parameterType="java.util.Map" resultType="JwCourseBaseInfo">
        SELECT
        <include refid="Base_Column_List" />
        FROM saas_jw_course_base_info WHERE
        ${property} = #{value}
        LIMIT 0,1
    </select>

    <select id="findList" parameterType="java.util.Map" resultType="JwCourseBaseInfo">
        SELECT
        <include refid="Base_Column_List" />
        FROM saas_jw_course_base_info WHERE
        ${property} = #{value}
        <if test="orderBy!=null">
            ORDER BY ${orderBy} ${sortBy}
        </if>
    </select>

    <select id="findAll" resultType="JwCourseBaseInfo">
        SELECT
        <include refid="Base_Column_List" />
        FROM saas_jw_course_base_info
        <if test="orderBy!=null">
            ORDER BY ${orderBy} ${sortBy}
        </if>
    </select>

    <select id="queryPage" resultType="JwCourseBaseInfo">
        SELECT
        <include refid="Base_Column_List" />
        FROM saas_jw_course_base_info
        <where>

            <if test="condition.whereSql != null">
                and id in (#{condition.whereSql})
            </if>

            <if test="condition.id!=null">
                ${condition.groupOp}   id  ${condition.id.op}  #{condition.id.data}
            </if>
            <if test="condition.tnId!=null">
                ${condition.groupOp}   tn_id  ${condition.tnId.op}  #{condition.tnId.data}
            </if>
            <if test="condition.courseName!=null">
                ${condition.groupOp}   course_name  ${condition.courseName.op}  #{condition.courseName.data}
            </if>
            <if test="condition.courseType!=null">
                ${condition.groupOp}   course_type  ${condition.courseType.op}  #{condition.courseType.data}
            </if>
            <if test="condition.grade!=null">
                ${condition.groupOp}   grade  ${condition.grade.op}  #{condition.grade.data}
            </if>


    </where>

        <if test="orderBy!=null">
        ORDER BY ${orderBy} ${sortBy}
        </if>
        <if test="offset != null">
            limit ${offset}, ${rows}
        </if>
    </select>

    <select id="like" parameterType="java.util.Map" resultType="JwCourseBaseInfo">
        SELECT
        <include refid="Base_Column_List" />
        FROM saas_jw_course_base_info
        <trim prefix="WHERE" prefixOverrides="AND | OR">
            <if test="condition.id!=null">
                AND id like CONCAT('%', #{condition.id} , '%')
            </if>
            <if test="condition.tnId!=null">
                AND tn_id like CONCAT('%', #{condition.tnId} , '%')
            </if>
            <if test="condition.courseName!=null">
                AND course_name like CONCAT('%', #{condition.courseName} , '%')
            </if>
            <if test="condition.courseType!=null">
                AND course_type like CONCAT('%', #{condition.courseType} , '%')
            </if>
            <if test="condition.grade!=null">
                AND grade like CONCAT('%', #{condition.grade} , '%')
            </if>
        </trim>
        <if test="orderBy!=null">
        ORDER BY ${orderBy} ${sortBy}
        </if>
    </select>


    <select id="queryList" parameterType="java.util.Map" resultType="JwCourseBaseInfo">
        SELECT
        <include refid="Base_Column_List" />
        FROM saas_jw_course_base_info
        <trim prefix="WHERE" prefixOverrides="AND | OR">
            <if test="condition.id!=null">
                AND id = #{condition.id}
            </if>
            <if test="condition.tnId!=null">
                AND tn_id = #{condition.tnId}
            </if>
            <if test="condition.courseName!=null">
                AND course_name = #{condition.courseName}
            </if>
            <if test="condition.courseType!=null">
                AND course_type = #{condition.courseType}
            </if>
            <if test="condition.grade!=null">
                AND grade = #{condition.grade}
            </if>
        </trim>
        <if test="orderBy!=null">
        ORDER BY ${orderBy} ${sortBy}
        </if>
    </select>

    <select id="queryOne" parameterType="java.util.Map" resultType="JwCourseBaseInfo">
        SELECT
        <include refid="Base_Column_List" />
        FROM saas_jw_course_base_info
        <trim prefix="WHERE" prefixOverrides="AND | OR">
            <if test="condition.id!=null">
                AND id = #{condition.id}
            </if>
            <if test="condition.tnId!=null">
                AND tn_id = #{condition.tnId}
            </if>
            <if test="condition.courseName!=null">
                AND course_name = #{condition.courseName}
            </if>
            <if test="condition.courseType!=null">
                AND course_type = #{condition.courseType}
            </if>
            <if test="condition.grade!=null">
                AND grade = #{condition.grade}
            </if>
        </trim>
        limit 0,1
    </select>

    <select id="count" resultType="java.lang.Integer">
        SELECT count(id) FROM saas_jw_course_base_info
       <where>
           <if test="id!=null">
               ${groupOp} id ${id.op}  #{id.data}
           </if>
           <if test="tnId!=null">
               ${groupOp} tn_id ${tnId.op}  #{tnId.data}
           </if>
           <if test="courseName!=null">
               ${groupOp} course_name ${courseName.op}  #{courseName.data}
           </if>
           <if test="courseType!=null">
               ${groupOp} course_type ${courseType.op}  #{courseType.data}
           </if>
           <if test="grade!=null">
               ${groupOp} grade ${grade.op}  #{grade.data}
           </if>

       </where>
    <if test="orderBy!=null">
        ORDER BY ${orderBy} ${sortBy}
    </if>
    </select>

    <select id="selectMaxId" resultType="java.lang.Integer">
        SELECT
        MAX(id)
        FROM saas_jw_course_base_info
    </select>

</mapper>

